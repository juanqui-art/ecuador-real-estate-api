{
  "name": "@realty-core/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for realty-core",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "zod": "^4.0.5"
  },
  "peerDependencies": {
    "zod": "^4.0.5"
  }
}