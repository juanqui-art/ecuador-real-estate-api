{
  "name": "realty-core",
  "version": "2.0.0",
  "description": "Sistema de gestión inmobiliaria para Ecuador - Full Stack",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --parallel run build",
    "test": "pnpm --parallel run test",
    "lint": "pnpm --parallel run lint",
    "clean": "pnpm --parallel run clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}